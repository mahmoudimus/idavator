"""Vendored third-party code.

``idavator._vendor.clang`` is the LLVM ``libclang`` Python bindings (``cindex``),
carried in-tree so the AST-equivalence oracle (``idavator.oracle``) has no
external/sibling-repo dependency. See ``clang/LICENSE`` for its license.
"""
